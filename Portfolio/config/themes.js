export default {
    icons: {
        iconfont: 'mdiSvg' || 'mdi' || 'mdiSvg' || 'md' || 'fa' || 'fa4',
    },
    theme: {
        themes: {
            light: {},
            dark: {}
        }
    }
}